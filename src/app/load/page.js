import Loading from "@/src/components/Loading";

export default function Load () {
    return (
        <div className="w-scrreen h-screen flex flex-col items-center justify-center content-center">
            <Loading />
        </div>
    )
}